<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spam9700.spam.dao.ReservationDao">

    <!-- 좌석별 예약 조회 -->
    <select id="getReservationsBySeat" resultType="com.spam9700.spam.dto.ReservationDTO">
        SELECT * FROM reservation WHERE seat_id = #{seatId}
    </select>

    <!-- 예약 생성 -->
    <insert id="createReservation" parameterType="com.spam9700.spam.dto.ReservationDTO">
        INSERT INTO reservation (seat_id, customer_name, reservation_time, start_time, end_time, payment_method)
        VALUES (#{seatId}, #{customerName}, #{reservationTime}, #{startTime}, #{endTime}, #{paymentMethod})
    </insert>

</mapper>
